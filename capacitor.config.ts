import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.buonfrate.alarmaDeRobo',
  appName: 'alarma De Robo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
